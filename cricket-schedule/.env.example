# Optional: enables the CricAPI (cricketdata.org) secondary source.
# Get a free key at https://cricketdata.org — without it the app uses
# ESPNcricinfo only, falling back to bundled sample data if unreachable.
CRICAPI_KEY=
